---
name: query
description: |
  基于知识库 wiki 内容回答用户问题，执行完整的查询流程（判断领域、
  选择查询模式、读取索引和页面、综合回答并标注 [[引用]]）。
  当用户询问任何与知识库相关的问题、寻求信息总结、对比分析、
  概念解释、最佳实践建议时，**必须**使用此 skill。
  即使问题看似简单（如"Rust 所有权是什么"），也应优先查询 wiki 而非凭记忆回答，
  因为 wiki 可能包含用户个人化的笔记和最新 ingest 的内容。
  不要凭通用知识回答知识库相关问题，必须使用此 skill 查询 wiki 后作答。
  触发场景包括但不限于：概念解释、工具对比、最佳实践、知识总结、
  "知识库里关于 X 有什么"、跨领域综合问题、任何涉及 wiki 内容的提问等。
---

# Query — 知识库查询

基于知识库 `wiki/` 中的内容回答用户问题。优先使用 wiki 中的信息，
而非模型的训练数据，因为 wiki 包含用户个人的知识积累。

## 查询策略

根据领域规模自动选择查询方式：

| 场景 | 阈值 | 策略 |
|---|---|---|
| 小规模 | 涉及领域 wiki 页面数总和 ≤ 300 | index.md 模式 |
| 大规模 | 涉及领域 wiki 页面数总和 > 300 | qmd 搜索模式 |

> 页面数从顶层 `index.md` 中读取，每个领域标注了当前 wiki 页面数。

## 查询流程

### 1. 识别涉及领域

快速扫一眼知识库目录结构（`knowledge/` 下的领域文件夹）。

根据用户问题判断涉及哪些领域：
- 用户**显式指定**了领域（如"在 Rust 知识库里..."）→ 直接使用该领域
- 用户**未指定** → 根据关键词推断（如"所有权"→Rust，"RAG"→AI）
- 涉及**多个领域** → 同时查询多个
- 不确定 → 询问用户，不要猜测

### 2. 判断规模并选择模式

读取 `knowledge/index.md`，获取各领域的 wiki 页面数，计算涉及领域的页面总和：

```
小规模 (≤300 页):  并行读取各领域的 wiki/index.md，人工浏览定位页面
大规模 (>300 页):  先 qmd update，再用 qmd 搜索关键词定位页面
```

**为什么区分规模很重要**：
- 小规模时，wiki/index.md 通常只有几十行，人工浏览快速高效
- 大规模时，wiki/index.md 可能列出数百个页面，人工逐行浏览和筛选效率极低，且容易遗漏。此时 qmd 的 BM25 关键词搜索能精准定位相关内容
- **关键原则**：大规模领域不要直接读取 wiki/index.md 来手动筛选页面，应先用 qmd 搜索

### 3. 执行查询

#### index.md 模式（小规模）

1. **并行读取**涉及领域的 `wiki/index.md`
2. 从 index.md 中**定位最相关的页面链接**（按分类浏览）
3. **读取具体页面内容**
4. 综合信息，形成回答

#### qmd 模式（大规模）

**重要**：大规模领域下 wiki/index.md 可能包含数百个页面链接，不要直接读取 index.md 来人工筛选。

1. **执行 `qmd update`** — 确保索引最新（跨机器切换后索引可能过期）
2. **确定 collection** — 从各领域的 `CLAUDE.md` 中读取 qmd collection 名称
3. **执行搜索**（用用户问题中的关键词）：
   ```bash
   # 优先使用 qmd query（混合搜索，效果更好）
   qmd query "用户问题中的关键词" -c <collection1> -c <collection2> --full -n 10

   # 如果 query 不可用，回退到 search（纯 BM25）
   qmd search "用户问题中的关键词" -c <collection1> -c <collection2> --full -n 10
   ```
4. **读取搜索结果中的页面**
5. 如果 qmd 搜索结果不足，再考虑读取 wiki/index.md 作为补充
6. 综合信息，形成回答

> 搜索默认覆盖所有涉及的 collection。如果用户问题明确限定在某领域，只搜索该 collection。

### 4. 组织回答

回答结构：

```markdown
## [问题核心要点]

[综合回答，整合多个来源的信息]

### 关键要点
- ...
- ...

### 相关页面
- [[页面名]] — 简要说明相关点
- [[页面名]] — ...
```

**引用规范**：
- 回答中涉及的事实、观点必须标注来源，使用 `[[页面名]]` 格式
- 如果多个页面支持同一观点，引用最关键的 1-2 个
- 引用应自然融入句子，不要堆砌

**回答原则**：
- 基于 wiki 内容，不要引入训练数据中的信息（除非 wiki 中确实没有，可补充说明"知识库中未涉及，以下是补充..."）
- 如果 wiki 内容有矛盾，指出矛盾并说明各来源的立场
- 如果信息不足，明确说明"知识库中关于 X 的信息有限"
- 跨领域问题时，显式说明各领域的视角差异

### 5. 可选：归档为 wiki 页面

如果回答具有长期价值（如系统性的对比分析、综合结论、最佳实践总结），
可询问用户是否需要归档为新 wiki 页面：

- 概念解释 → `concepts/`
- 对比分析 → `comparisons/`
- 最佳实践/综合结论 → `syntheses/`
- 领域概览 → `overviews/`

归档时：
1. 创建新页面，包含标准 frontmatter（title, date, tags, source_count）
2. 内容精炼为可独立阅读的 wiki 页面
3. 更新该领域的 `wiki/index.md` 添加链接
4. 在 `wiki/log.md` 记录 Query 操作

## 规模阈值速查

| 查询类型 | 小规模 | 大规模 |
|---|---|---|
| 单领域 | ≤ 300 页 | > 300 页 |
| 多领域 | 总和 ≤ 300 页 | 总和 > 300 页 |

当前各领域的页面数见 `knowledge/index.md`。
