---
title: minigrep 项目目标
date: 2026-07-17
tags:
  - Rust
  - 项目
  - minigrep
aliases:
  - minigrep 项目目标
  - Rust minigrep
---

# 一、项目目标

本项目实现一个简化版 `grep`，命名为 `minigrep`。

`grep` 的基本功能：

> 在指定文件中搜索指定文本，并打印出所有包含该文本的行。

`minigrep` 项目只实现最基础的搜索能力，使用如下：

```shell
cargo run <query> <filename>
```

其中：

- `query` 是要搜索的文本。
- `filename` 是指定文件。

例如：

```shell
cargo run frog poem.txt
```

含义是：在 `poem.txt` 文件中搜索包含 `frog` 的行。

这个项目会用到几个常见的 Rust 命令行程序知识点：

| 知识点 | 用途 |
|---|---|
| 命令行参数 | 从终端接收搜索内容和文件名 |
| 文件读取 | 读取指定文件的文本内容 |
| 字符串处理 | 在文本中查找匹配行 |
| 错误处理 | 处理参数不足、文件不存在等情况 |
| 模块拆分 | 将入口代码和业务逻辑分开 |
| 测试 | 验证搜索函数是否正确 |

第一版学习路径可以拆成以下几个阶段：

1. 在 [[Rust/notes/项目/01_minigrep/2、命令行参数与文件读取|命令行参数与文件读取]] 中，先读取命令行参数，并把文件内容读取出来。
2. 在 [[Rust/notes/项目/01_minigrep/3、项目重构与错误处理|项目重构与错误处理]] 中，把入口、配置、运行逻辑和错误处理拆开。
3. 在 [[Rust/notes/项目/01_minigrep/4、搜索功能、环境变量与输出流|搜索功能、环境变量与输出流]] 中，实现真正的搜索函数，并补充测试、环境变量和标准错误输出。

本篇只负责明确 `minigrep` 的项目目标和学习边界。后续笔记会按真实开发流程逐步完成命令行参数、文件读取、重构、错误处理、搜索逻辑和测试。
