# 一、模式匹配的基本概念

模式匹配是 Rust 中非常核心的控制流能力。它不仅可以判断一个值属于哪一种情况，还可以在匹配成功时把值内部的数据取出来，绑定到变量上继续使用。

最常见的模式匹配形式是`match`：

```rust
enum Msg {
    Text(String),
    Ping,
    Move { x: i32, y: i32 },
}

fn describe(msg: Msg) -> String {
    match msg {
        Msg::Text(text) => format!("text: {}", text),
        Msg::Ping => "ping".to_string(),
        Msg::Move { x, y } => format!("move to {}, {}", x, y),
    }
}
```

`=>`左边是模式，右边是匹配成功后执行的表达式。

在上述例子中：

- `Msg::Ping` 匹配不带数据的枚举变体
- `Msg::Text(text)` 匹配带一个数据的变体，并将内部`String`绑定到`text`
- `Msg::Move { x, y }` 匹配结构体风格的枚举变体，并把字段绑定到`x`和`y`

很多模式看起来像创建值的语法，这是 Rust 有意设计的：**表达式用来创建值，模式用来拆开值、检查形状并提取数据**。

`match`会从上到下依此尝试每个分支，第一个匹配成功的分支会被执行，后面的分支不再检查。因此，分支顺序很重要，**更具体的模式要放在更前面**。

示例：

```rust
let x = 10;

match x {
    n => println!("{}", n),
    1 => println!("one"),
}
```

这里的 `n` 是变量模式，可以匹配任何值，所以第二个分支永远不会被执行。编译器会给出警告 `unreachable pattern`，表示该模式不可达。


# 二、match表达式

## 1、match的基本形式

`match`的基本形式如下：

```rust
match value {
  pattern1 => expression1,
  pattern2 => expression2,
  pattern3 => expression3,
}
```

每个分支由一个模式和一个表达式组成。分支表达式较短时，通常直接写在`=>`后面，并以逗号`,`结尾。

示例：

```rust
fn describe_number(n: i32) -> &'static str {
	match n {
		0 => "zero",
		1 => "one",
		_ => "other",
	}
}
```

如果分支中需要执行多条语句，可以使用代码块：

```rust
fn print_number(n: i32) {
    match n {
        0 => println!("zero"),
        x => {
            println!("number:");
            println!("{}", x);
        }
    }
}
```

`match` 本身是表达式，所以各个分支的返回类型必须一致。

示例：

```rust
fn is_zero(n: i32) -> bool {
    match n {
        0 => true,
        _ => false,
    }
}
```


## 2、match的穷尽性检查

Rust 要求 `match` 必须覆盖所有可能情况，这称为穷尽性检查。

```rust
fn describe(n: i32) -> &'static str {
    match n {
        0 => "zero",
        1 => "one",
    }
}
```

这段代码无法通过编译，因为 `i32` 还有很多其他可能值。通常可以使用 `_` 作为兜底分支：

```rust
fn describe(n: i32) -> &'static str {
    match n {
        0 => "zero",
        1 => "one",
        _ => "other",
    }
}
```

`_`是通配符模式，表示匹配任何值，但不绑定变量。

穷尽性检查是 Rust 模式匹配的重要安全机制。它能减少遗漏分支造成的逻辑错误，尤其适合处理枚举。


# 三、常见模式

## 1、字面量模式、变量模式和通配符模式
### （1）字面量模式

字面量模式用于精确匹配某个值 。

示例：

```rust
fn traffic_light(color: &str) -> &'static str {
    match color {
        "red" => "stop",
        "green" => "go",
        "yellow" => "slow down",
        _ => "unknown",
    }
}
```

### （2）变量模式

变量模式用于匹配任何值，并把值绑定到变量。

示例：

```rust
fn show(n: i32) {
    match n {
        0 => println!("zero"),
        x => println!("non-zero: {}", x),
    }
}
```

这里的 `x` 会匹配所有没有被前面分支处理的值，并把值绑定到 `x`。


### （3）通配符模式

通配符模式 `_` 也能匹配任何值，但不会绑定变量。

示例：

```rust
fn maybe_print(n: i32) {
    match n {
        0 => println!("zero"),
        _ => {}
    }
}
```

变量模式和 `_` 的区别在于：变量模式会拿到匹配到的值，`_` 只是忽略它。

## 2、元组模式

元组模式适合一次匹配多个位置。

示例：

```rust
fn describe_point(x: i32, y: i32) -> &'static str {
    match (x, y) {
        (0, 0) => "origin",
        (_, 0) => "on the x axis",
        (0, _) => "on the y axis",
        (_, _) => "somewhere else",
    }
}
```

`(_, 0)` 表示第一个位置不关心，第二个位置必须是 `0`。

元组模式常用于**将多个值组合起来一起判断**。

```rust
fn compare(a: i32, b: i32) -> &'static str {
    match (a > 0, b > 0) {
        (true, true) => "both positive",
        (true, false) => "only a is positive",
        (false, true) => "only b is positive",
        (false, false) => "neither is positive",
    }
}
```


## 3、结构体模式

结构体模式按字段名匹配和绑定。

示例：

```rust
struct Point {
    x: i32,
    y: i32,
}

fn describe(point: Point) -> String {
    match point {
        Point { x: 0, y } => format!("on y-axis, y = {}", y),
        Point { x, y } => format!("at ({}, {})", x, y),
    }
}
```

`Point { x: 0, y }` 表示 `x` 必须等于 `0`，同时把 `y` 字段绑定到变量 `y`。

字段名和变量名不一样时，可以写成：

```rust
Point { x: a, y: b }
```

如果变量名和字段名相同，可以简写为：

```rust
Point { x, y }
```

当结构体字段很多，而模式只关心其中一部分时，可以使用 `..` 忽略其他字段。

示例：

```rust
struct User {
    name: String,
    age: u8,
    city: String,
}

fn show_name(user: User) {
    match user {
        User { name, .. } => println!("name = {}", name),
    }
}
```

`User { name, .. }` 表示绑定 `name` 字段，忽略 `age` 和 `city`。

需要注意，如果被绑定的字段不是`Copy`类型，例如`String`，那么这里会把字段移动出来。移动后，原来的结构体会发生部分移动，不能再作为完整值继续使用。

## 4、枚举模式

枚举模式是 Rust 模式匹配最常见的使用场景之一。

```rust
enum Command {
    Quit,
    Echo(String),
    Move { x: i32, y: i32 },
}

fn run(command: Command) {
    match command {
        Command::Quit => println!("quit"),
        Command::Echo(text) => println!("{}", text),
        Command::Move { x, y } => println!("move to {}, {}", x, y),
    }
}
```

不同枚举变体可以携带不同形状的数据，模式匹配可以根据变体种类分别处理，并取出内部数据。

`Option<T>`和`Result<T,E>`本质也是枚举，所以他们也经常和模式匹配一起使用。

```rust
fn print_value(value: Option<i32>) {
    match value {
        Some(n) => println!("{}", n),
        None => println!("no value"),
    }
}
```

```rust
fn parse_number(text: &str) -> i32 {
    match text.parse::<i32>() {
        Ok(n) => n,
        Err(_) => 0,
    }
}
```

`match` 非常适合处理这种“有几种明确情况，每种情况处理方式不同”的逻辑。


## 5、数组模式和切片模式

数组和切片都可以使用`[...]`形式进行模式匹配。

数组长度固定，所以**数组模式按固定位置匹配**。

示例：

```rust
fn hsl_to_rgb(hsl: [u8; 3]) -> [u8; 3]  {
	match hsl {
	  [_, _, 0] => [0, 0, 0],
	  [_, _, 255] => [255, 255, 255],
	  ...
	}
}
```

将HSL（色相、饱和度、亮度）颜色值转换为RGB（红色、绿色、蓝色）颜色值时，具有零亮度或全亮度的颜色只会是黑色或白色。

切片长度可变，可以配合`..`匹配剩余元素。

示例：

```rust
fn greet(names: &[&str]) {
    match names {
        [] => println!("Hello, nobody."),
        [one] => println!("Hello, {}.", one),
        [first, second] => println!("Hello, {} and {}.", first, second),
        [first, .., last] => println!("Hello, {} ... {}.", first, last),
    }
}
```

这里`names`类型是`&[&str]`，所以`one`、`first`、`last` 的类型是 `&&str`。这是因为**匹配的是一个切片引用**，模式中绑定到的是切片元素的引用。

切片模式适合处理“开头、结尾、长度不同”的数据结构，例如命令参数、路径片段、Token 列表等。

# 四、模式中的绑定与借用

## 1、默认绑定可能发生移动

**模式匹配不只是判断，还会绑定变量**。绑定变量时，Rust会遵守所有权规则。

如果匹配的是一个拥有所有权的值，并且绑定了其中的非`Copy`字段，那么该字段会被移动出来。

示例：

```rust
struct User {
    name: String,
    lang: String,
    age: u8,
}

fn demo(user: User) {
    match user {
        User { name, lang, .. } => {
            println!("{} uses {}", name, lang);
        }
    }
}
```

这里的 `name` 和 `lang` 是 `String`，不是 `Copy` 类型，因此会被移动到分支内部的局部变量中。

如果只移动了结构体的一部分字段，原结构体会发生部分移动。之后不能再把它作为完整值使用。


## 2、ref和ref mut模式

如果只是想借用字段，而不是把字段移动出来，可以使用`ref`。

示例：

```rust
struct User {
    name: String,
    lang: String,
    age: u8,
}

fn demo(user: User) {
    match user {
        User { ref name, ref lang, .. } => {
            println!("{} uses {}", name, lang);
        }
    }
}
```

`ref name` 表示不要把 `name` 字段移动出来，而是借用它。此时 `name` 的类型是 `&String`。

如果需要可变借用字段，可以使用 `ref mut`。

```rust
#[derive(Debug)]
struct User {
    name: String,
    lang: String,
    age: u8,
}

fn demo(mut user: User) {
    match user {
        User { ref name, ref mut lang, .. } => {
            lang.clear();
            lang.push_str("Rust");

            println!("name: {}, lang: {}", name, lang);
        }
    }

    println!("{:?}", user);
}
```

要使用 `ref mut`，被匹配的值本身必须允许可变借用，所以这里的参数写成了 `mut user`。

`ref` 和 `ref mut` 出现在模式中，它们不是创建引用的表达式，而是改变绑定方式：从“移动绑定”变成“借用绑定”。

## 3、引用模式

**`&`出现在表达式中表示创建引用，出现在模式中表示匹配引用**。

- 表达式： `&v`产生`&T`
- 模式：`&pat`只匹配`&T`类型的值。匹配成功后，Rust 会把这个引用“解开一层”，然后再拿引用里面的值去匹配 `pat`。

示例：

```rust
let x = 10;
let r = &x;

match r {
	&n => println!("{}", n),
}
```

这里 `r` 的类型是 `&i32`，模式 `&n` 会匹配这个引用，并把引用指向的 `i32` 复制到 `n` 中。因为 `i32` 是 `Copy` 类型，所以这样写没问题。

结构体也可以使用引用模式：

```rust
struct Point {
    x: i32,
    y: i32,
}

fn show(point: &Point) {
    match point {
        &Point { x, y } => {
            println!("({}, {})", x, y);
        }
    }
}
```

这里 `x` 和 `y` 是 `i32`，所以可以从共享引用中复制出来。

但如果**字段不是 `Copy` 类型，就不能通过共享引用把字段移动出来**。

示例：

```rust
#[derive(Debug)]  
struct User {  
    name: String,  
    lang: String,  
    age: u8,  
}  
  
  
fn demo(user: &User) {  
  
    match user {  
        &User { name, lang, .. } => {  
            println!("name: {}, lang: {}", name, lang)  
        }  
    }  
  
}
```

上述代码会报错：

```shell
error: cannot move out of `user.name` which is behind a shared reference
```

可以这样解决：

```rust
struct User {
    name: String,
    lang: String,
}

fn show(user: &User) {
    match user {
        &User { ref name, ref lang } => {
            println!("{} uses {}", name, lang);
        }
    }
}
```

这里必须用 `ref` 借用 `name` 和 `lang`，不能直接移动它们。




&在Option/迭代器里很常见。当拿到的是Option<&T>，常见的写法是Some(&x)：Some(&x)只在T: Copy时特别顺手，因为它相当于是把T复制出来。

示例：
```rust
fn demo(next: Option<&char>) {

match next {

Some(&c) => println!("next char = {}", c),

None => println!("end"),

}

}
```
如果T不是Copy（比如String），一般匹配成Some(s)，让s保持引用类型，而不是转移所有权。
# 三、匹配守卫

有时候模式本身匹配成功还不够，还需要额外条件为真，这个额外条件就称为匹配守卫（match guard）。

**匹配守卫的写法就是在模式和=>之间加上if条件：**

```rust
PATTERN if CONDITION => EXPR
```
匹配流程：

先判断值是否匹配 PATTERN；

若匹配成功，再计算 CONDITION；

只有条件为真才执行该分支；否则该分支视为不匹配，继续尝试下一个分支

示例：
```rust
fn choose_page(current: u32, clicked: Option<u32>) -> Result<u32, &'static str> {

match clicked {

None => Err("not on a page"),

Some(p) => {

if p == current { Err("you clicked the current page") }

else { Ok(p) }

}

}

}
```
用匹配守卫，可改写为：
```rust
fn choose_page(current: u32, clicked: Option<u32>) -> Result<u32, &'static str> {

match clicked {

None => Err("not on a page"),

Some(p) if p == current => Err("you clicked the current page"),

Some(p) => Ok(p),

}

}
```
# 四、匹配多种可能性

有时希望一个分支能匹配多种情况。Rust提供了两种常用写法：

**或模式：** pat1 | pat2 | pat3

**范围模式：** start..=end（含端点），start..end（不含端点）

### （1）或模式

用|将多个情况合并为一个分支。

```text
fn is_vowel(c: char) -> bool {

'a' | 'e' | 'i' | 'o' | 'u' => true,

_ => false,

}
```
这样写的好处是： **多个分支返回同样的结果时，可以合并** ，减少重复代码。

|可以与解构/引用模式一起用。

示例：
```rust
fn at_end(peeked: Option<&char>) -> bool {

match peeked {

Some(&'\\n') | Some(&'\\r') | None => true,

_ => false,

}

}
```
### （2）范围模式

范围模式匹配一个区间的值。start..=end表示匹配从start到end之间的所有值，包括端点。start..end表示匹配start到end-1之间的所有值，不含端点。

示例：

```rust
fn classify(c: char) -> &'static str {

match c {

'0'..='9' => "digit",

'a'..='z' | 'A'..='Z' => "letter",

' ' | '\\n' | '\\t' => "whitespace",

_ => "other",

}

}
```
此外，还有 **x..**的形式，表示匹配从x开始直到该类型最大值的所有值。

示例：
```rust
fn level(score: u32) -> &'static str {

match score {

0..=59 => "fail",

60..=89 => "pass",

90.. => "excellent", // 90 到 u32::MAX

}

}
```
# 五、使用@模式绑定

**x @ pattern** 的作用是：

先用pattern去匹配

匹配成功时，把“整个匹配的值”绑定到变量x

同时仍然可以在pattern内部进行更细的匹配

可以将其理解为： **既想按照某个模式匹配和检查又想把原值整体保存下来** 。

示例：

```rust
#[derive(Debug)]

enum Msg {

Text(String),

Ping,

}

fn handle(m: &Msg) {

println!("{:?}", m);

}

fn demo(m: Msg) {

match m {

whole @ Msg::Text(_) => handle(&whole),

_ => {}

}

}
```
**@对范围模式特别有用，既可以检查范围又可以拿到值** ：
```rust
fn read_token(c: char) -> String {

match c {

num @ '0'..='9' => format!("digit: {}", num),

_ => "other".to_string()

}

}
```
# 六、简洁控制流

## 1、if let

当只关心一种模式时，使用match会显得有些啰嗦：必须写一个兜底分支。if let就是为这种场景提供的一种简洁写法。

基本形式：

```rust
if let PATTERN = EXPR {

// 匹配成功：这里可以使用PATTERN绑定的变量

}

EXPR匹配了PATTERN：进入代码块，并完成变量绑定
```
不匹配：跳过此块，什么也不做

示例：
```rust
let some_value = Some(10u8);

if let Some(val) = some_value {

println!("Got val {}", val);

}
```
这等价于：
```rust
let some_value = Some(10u8);

match some_value {

Some(val) => println!("Got val {}", val),

_ => {},

}
```
因此， **if let可以理解为match的语法糖：只写关心的模式，其他情况自动忽略** 。

如果还是想处理不匹配的情况，还可以增加else分支：
```rust
let some_value = Some(10u8);

if let Some(val) = some_value {

println!("Got val {}", val);

} else {

println!("No val");

}
```
## 2、let else

**let else用于匹配失败就立即退出的场景，可以显著减少嵌套，让主流程更清楚** 。

基本语法：

```rust
let PATTERN = EXPR else {

// 匹配失败时执行

// 这里必须提前退出：return / break / continue / panic!

}
```
若EXPR匹配PATTERN：把绑定写入变量，继续向下执行

若不匹配，执行else块，并且必须在块内提前退出（编译器会强制这一点）

示例1:

Option失败就return。
```rust
fn process(value: Option<i32>) {

let Some(v) = value else {

return;

};

println!("{}", v);

}
```
示例2:

Result失败就panic（或返回错误）。
```rust
fn parse(text: &str) -> i32 {

let Ok(num) = text.parse::<i32>() else {

panic!("not a number");

};

num

}
```
## 3、while let

while let是把 **循环 + 模式匹配** 合在一起的简洁写法：只要匹配成功就继续循环；匹配失败就退出循环。它特别适合不断尝试获取下一个值，直到没有/失败为止的流程。

基本语法：

```rust
while let PATTERN = EXPR {

// 循环体：可以使用PATTERN绑定的变量

}
```
执行规则：
