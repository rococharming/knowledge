---
title: 单元测试
date: 2026-06-01
tags: [rust, testing, unit-test]
source_count: 1
---

# 单元测试

单元测试用于测试一个较小的代码单元，例如一个函数、一个模块或某个内部逻辑。

## 核心特点

Rust 的单元测试一般与被测试代码**放在同一个源码文件**，这样做有两个好处：

1. 测试代码靠近被测试代码，便于维护
2. 单元测试可以访问当前模块中的**私有项**

单元测试更适合验证局部逻辑，例如边界条件、私有辅助函数、错误分支和纯函数行为。

## 测试模块与 #[cfg(test)]

单元测试的常见写法是在源码文件中定义一个 `tests` 模块：

```rust
pub fn add_two(value: i32) -> i32 {
    internal_add(value, 2)
}

fn internal_add(left: i32, right: i32) -> i32 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn add_two_adds_two() {
        let result = add_two(2);
        assert_eq!(result, 4);
    }
}
```

`#[cfg(test)]` 的作用：只有在测试构建时才编译这个模块。普通的 `cargo build` 不会编译该测试模块。

- `cfg` 是 configuration 的缩写，表示条件编译
- `test` 是 Rust 在编译测试时提供的配置选项

## 测试私有函数

因为单元测试模块通常是当前模块的子模块，所以它可以通过 `use super::*;` 访问父模块中的私有项。

```rust
fn internal_add(left: i32, right: i32) -> i32 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn internal_add_works() {
        let result = internal_add(2, 2);
        assert_eq!(result, 4);
    }
}
```

`internal_add` 没有使用 `pub`，但单元测试仍然可以调用它。

## 是否测试私有函数

| 场景 | 建议 |
| --- | --- |
| 私有函数逻辑复杂，且难以通过公开接口覆盖 | 可以直接测试 |
| 私有函数只是公开函数的简单拆分 | 优先测试公开接口 |
| 私有函数经常变化 | 谨慎直接测试，避免测试过度依赖实现细节 |

## 来源

- [[测试]]
