---
title: 算术、赋值与运算顺序
date: 2026-08-11
tags: [Web, 前端, JavaScript, 运算符]
aliases:
  - JavaScript算术运算符
  - JavaScript赋值运算符
  - 运算符优先级
---

# 一、算术表达式

算术运算符接收操作数并计算出新值。`+` 在两个操作数都是数字时做加法；字符串参与 `+` 时的转换规则见 [[9、类型转换与数值处理|类型转换与数值处理]]。

| 运算符 | 含义 | 示例 |
|---|---|---|
| `+` | 加法 | `10 + 5` 为 `15` |
| `-` | 减法 | `10 - 5` 为 `5` |
| `*` | 乘法 | `10 * 5` 为 `50` |
| `/` | 除法 | `10 / 4` 为 `2.5` |
| `%` | 余数 | `10 % 3` 为 `1` |
| `**` | 幂 | `2 ** 3` 为 `8` |

`%` 是余数运算符，不是数学上的通用模运算；结果符号通常跟左操作数一致。

```js
console.log(-10 % 3); // -1
console.log(10 / 0);  // Infinity
console.log(0 / 0);   // NaN
```

# 二、赋值与复合赋值

`=` 把右侧计算出的值保存到左侧变量。复合赋值将“计算再赋值”写得更短。

```js
let score = 10;

score += 5; // 等同于 score = score + 5
score -= 2;
score *= 3;
score /= 2;
score %= 4;
score **= 2;
```

复合赋值也有位运算和逻辑运算版本；先理解对应的普通运算符，再使用缩写。

# 三、自增与自减

`++` 和 `--` 只能用于可写变量。前缀先修改再取值，后缀先取旧值再修改；单独写成一条语句时两者效果相同。

```js
let a = 5;
const before = a++;

let b = 5;
const after = ++b;

console.log(before, a); // 5, 6
console.log(after, b);  // 6, 6
```

在复杂表达式里混用 `++`、`--` 会降低可读性。能写成独立的一次更新时，优先拆开写。

# 四、优先级与结合性

表达式中优先级高的运算先执行：乘除与幂先于加减。括号可以明确覆盖默认顺序，应优先用于表达业务意图，而不是死记完整优先级表。

```js
console.log(2 + 3 * 4);   // 14
console.log((2 + 3) * 4); // 20
```

同优先级的运算符还要看结合性。减法通常从左到右，幂运算从右到左：

```js
console.log(10 - 2 + 3); // 11
console.log(2 ** 3 ** 2); // 512，即 2 ** (3 ** 2)
```
