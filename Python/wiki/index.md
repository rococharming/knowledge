---
title: Python Wiki 索引
date: 2026-07-01
---

# Python Wiki 索引

## Summaries

- [[变量和数据类型]] — 变量赋值、命名规则、动态类型、常见内置类型、`type()` 与 `isinstance()`
- [[字符串]] — 字符串定义、不可变性、常用操作、`str` 与 `bytes`、编码解码、三种格式化、切片
- [[数字和数学运算]] — int/float、算术运算符、地板除、浮点精度、数值转换与 `round`/`abs`/`pow`
- [[条件判断]] — if 结构、Truthy/Falsy、布尔运算符与短路求值、三元表达式
- [[函数]] — def 定义、参数体系（位置/默认/可变/命名关键字/关键字）、返回值、LEGB 作用域
- [[列表和元组]] — 序列共性、list 方法、tuple 不可变精确含义、解包与选型
- [[循环]] — for/while、break/continue/else、range/enumerate/zip、列表推导式与函数式工具
- [[字典和集合]] — dict 增删改查、set 集合运算、可哈希对象
- [[模块]] — 标准库、import 方式、`__name__` 与 `if __name__ == '__main__'`
- [[异常处理]] — 错误类型、try-except、raise、异常链、自定义异常、assert
- [[类和对象]] — class 定义、`__init__`/self、类属性/实例属性、特殊方法、动态属性
- [[封装和属性]] — 封装含义、访问约定（`_`/`__`）、Getter/Setter、Property 装饰器、方法 vs Property
- [[继承和多态]] — 单/多继承、方法重写与 super、MRO、鸭子类型与多态、自定义异常层次

## Entities

_（暂无）_

## Concepts

- [[数据类型系统]] — 动态类型、内置类型分类、可变/不可变、可哈希、类型检查
- [[控制流]] — 条件判断与循环构成的执行流程、短路求值、三元、推导式
- [[容器类型]] — list/tuple/dict/set/range 选型、共性操作、可变边界
- [[字符串与编码]] — `str` vs `bytes`、编码解码、源文件编码、三种格式化
- [[字符串转换标志]] — `!r`/`!s`/`!a` 转换标志：在 f-string 和 `str.format()` 中控制值的呈现形式
- [[函数与作用域]] — 参数体系组合顺序、返回值、LEGB 作用域与 nonlocal/global
- [[面向对象]] — 类与实例、特殊方法、动态属性、三大特性、自定义异常、模块中的类
- [[封装]] — 封装目的、访问约定（`_`/`__`）、名称改写、Property 受控访问、方法 vs Property
- [[继承]] — 单/多继承、方法重写、`super()`、MRO、继承检查、自定义异常层次
- [[多态]] — 统一接口、鸭子类型（主要方式）、基于继承的多态、减少类型判断

## Comparisons

_（暂无）_

## Overviews

_（暂无）_

## Syntheses

_（暂无）_

## Recipes

_（暂无）_
