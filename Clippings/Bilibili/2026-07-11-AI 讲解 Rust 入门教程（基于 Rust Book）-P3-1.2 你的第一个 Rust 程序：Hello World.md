---
title: "AI 讲解 Rust 入门教程（基于 Rust Book）"
url: "https://www.bilibili.com/video/BV1uUDDBnE5x/?p=3"
author: "软件工艺师"
created: "2026-07-11"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116391348935202&bvid=BV1uUDDBnE5x&cid=37447730684&page=3&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

Google AI 来讲解 Rust 入门教程，使用的是 NotebookLM 基于 Gemini 3.1 Pro.
（基于官方的 Rust Book）

## 字幕

`00:00` 嘿大家好
`00:01` 想学一门新的编程语言
`00:03` 那咱们肯定得从那个最经典最传统的仪式开始
`00:07` 对吧
`00:07` 就是让电脑对我们说出那句你好世界
`00:11` 今天呢我们就来用rust完成这个仪式
`00:14` 这就算是你正式踏入rust世界的第一步了
`00:18` 没错我们的目标就这么简单
`00:20` 让屏幕上出现这几个字
`00:22` 你可别小看这一小步哦
`00:24` 它虽然看起来简单
`00:25` 但实际上呢他把创建编译
`00:28` 再到运行一个完整的RUS程序
`00:30` 这整个流程的核心环节都给咱们走了一遍
`00:34` 所以说意义还是挺重大的好
`00:36` 那在咱们动手写代码之前
`00:39` 得先给我们的项目找个家对吧
`00:41` 所以第一件事就是给我们的第一个程序
`00:44` 建一个他自己的小目录行
`00:47` 那你打开你的终端
`00:48` 然后敲入这个命令
`00:51` 你看MKDIR斜线项目文件
`00:55` 这个命令就会帮你创建一个项目文件夹
`00:58` 说实话啊
`01:00` rust这门语言它本身并不关心你把代码放哪儿
`01:04` 但咱们从一开始就养成好习惯
`01:07` 把东西放得整整齐齐的
`01:10` 绝对能让你后面省不少事儿
`01:12` 所以你看准备工作就两步
`01:14` 第一步文件夹我们刚刚建好了
`01:17` 那第二步呢
`01:18` 就是在这个文件夹里创建一个源文件
`01:21` 这里你得记住啊
`01:22` 所有rust的源文件
`01:24` 它的文件名结尾都必须是点rs
`01:27` 这个是规定
`01:28` 当当当当
`01:29` 这就是我们第一个程序的全部代码了
`01:32` 你看就三行是不是超简单
`01:35` 现在你就打开刚才创建的那个main点rs文件
`01:39` 把这三行代码原封不动的敲进去
`01:42` 然后保存
`01:43` 我知道它看起来简单
`01:45` 但其实每一部分都有自己的门道
`01:47` 不过别急
`01:48` 咱们马上就一行一行的把它掰开
`01:51` 揉碎了
`01:51` 讲清楚好了
`01:53` 代码也写完了
`01:54` 现在就到了解密时间
`01:56` 咱们一起来解剖一下这段代码
`02:03` 我们来看第一行f m main这个FNNA就是function
`02:09` 也就是函数的缩写
`02:11` 它告诉rust
`02:12` 嘿
`02:13` 我要定义一个函数了
`02:15` 而main这个名字呢非常非常特殊
`02:18` 你可以把它理解成是整个城区的正门
`02:20` 或者说入口
`02:22` 不管你程序有多复杂
`02:24` rust总是从这个main函数开始执行
`02:27` 哦对了
`02:28` 后面这对空括号f and main括号
`02:31` 意思就是这个函数不需要任何输入
`02:34` 自己就能跑
`02:35` 然后呢就是这对大括号或者叫花括号
`02:39` 它们就像一个框框
`02:40` 把main函数要做的事情全都框在里面
`02:43` 换句话说
`02:44` 写在这对括号里的所有代码都属于main函数
`02:48` 计算机会从上到下一行一行地执行它们好
`02:52` 终于到了核心部分
`02:55` 就是这行print line
`02:56` 他负责干了所有的体力活儿
`02:59` 也就是把hello world打印出来
`03:01` 但是你有没有注意到一个很有意思的小细节
`03:04` 就是print line后面居然跟了个感叹号
`03:08` 这是干嘛的呀
`03:09` 对吧
`03:10` 为什么print line后面偏偏要加个感叹号呢
`03:14` 这肯定不是为了表达激动的心情吧
`03:16` 它跟我们平时看到的那些普通函数
`03:19` 到底有什么不一样的地方
`03:21` 答案来了
`03:22` 这个感叹号其实是个信号
`03:24` 它告诉我们我们调用的这个print line啊
`03:27` 它不是一个普普普通通的函数
`03:29` 而是一个更厉害的东西
`03:31` 叫做rust红嗯
`03:33` 红是什么呢
`03:34` 现在你可以先简单的把它想象成
`03:36` 一种代码的模板
`03:37` 或者说超级快捷方式
`03:39` 他能做一些普通函数做不到的事情
`03:42` 功能特别强大
`03:43` 这个你先有个印象就行
`03:44` 一会咱们会经常和他打交道的
`03:47` 最后咱们看这一行的末尾
`03:49` 这个分号它的作用就像我们写文章时的句号
`03:53` 意思就是OK我这条指令说完了
`03:56` 他是在告诉编译器这个表达式到这里就结束了
`04:00` 你可以去看下一句了
`04:01` 在rust里
`04:02` 你会发现绝大多数代码行的结尾都有它
`04:06` OK理论学习结束
`04:08` 现在最激动人心的时刻到了
`04:11` 代码已经写好了
`04:12` 咱们也弄明白每一行是啥意思了
`04:14` 接下来就该让它真正的火起来编译
`04:18` 然后运行在这之前
`04:20` 我得先跟你说个概念
`04:22` rust是一门预编译语言
`04:25` 这是什么意思呢
`04:27` 就是说它跟Python或者JAVASCRIPT那种
`04:31` 你写完代码直接就能跑的语言不太一样
`04:34` 我们得先多一个步骤
`04:36` 用一个叫做编译器的工具
`04:39` 把我们写的这个dot rs源文件翻译成电脑
`04:43` 能直接看懂和执行的程序
`04:45` 所以啊整个过程就分成了清晰的两步
`04:49` 第一步叫编译
`04:50` 你在终端里输入rusty main drs
`04:54` 然后回车
`04:55` 这个命令就会召唤rust编译器
`04:57` 把我们的源代码变成一个可执行文件
`05:00` 然后是第二步运行
`05:02` 你再输入dash main
`05:04` 运行我们刚刚编译出来的那个文件
`05:07` 然后见证奇迹的时刻就到了
`05:10` 看到了吗
`05:11` 如果一切顺利
`05:12` 你的端端上就会清清楚楚地显示出hello world哇
`05:17` 这种感觉是不是超棒
`05:18` 亲手写的代码成功运行了
`05:21` 这就证明你的第一个程序完美无缺
`05:25` 那么恭喜你
`05:26` 你已经正式的完整的写出了
`05:29` 你的第一个rust的程序没错
`05:32` 从创建文件到编写代码
`05:35` 再到编译运行
`05:36` 你已经成功地走完了整个流程
`05:39` 所以从现在开始
`05:41` 你就可以自豪的说我是一名rust程序员了
`05:44` 欢迎你的加入哦好了
`05:47` 既然已经跟这个世界打过招呼了
`05:49` 那下一步呢你打算用rust来创造点什么
`05:53` 记住这仅仅是个开始
`05:56` 前面还有一整个充满无限可能的世界
`05:59` 在等着你探索呢

## 评论

1. 心之拼图（赞 166）
我已经入门rust 2.5次了，你也来试试吧[良辰共此曲动态表情包_泣]

2. 你能嫁我吗（赞 75）
如今，没有历史包袱的新项目在 AI 的加持下，正越来越多地倒向 Rust。连入门教程都能由 AI 直接生成，Rust 曾经极其陡峭的学习曲线，硬生生被 AI 填平了。作为一名嵌入式开发者，我现在感觉自己更像是一个“高精度传感器”，我对需求的描述精度，直接决定了 AI 产出代码的质量。只要我能清晰地界定出系统架构和实现方案，Gemini 3.1 Pro 基本能一次性吐出让我满意且完美运行的 Rust 代码。以前，我把大部分时间都耗在死磕语法细节和边界条件上，花点时间也能解出 LeetCode 的中等题。而现在，我的核心精力全部转移到了定义现实问题和推演架构上，反而连一些简单的算法题都有些手生了。这是一种很奇妙的感觉：我能敏锐地判断出一个变量的生命周期是否合理，却一时半会儿手写不出那段完美的生命周期标注代码了。我越来越感觉到，在未来，开发者的核心壁垒将不再是倒背如流的语言特性，而是对物理世界、硬件边界与业务逻辑的精准洞察。

3. 我正虚度年华（赞 28）
男人一生只哭99次：钟离假死、雷神与影、芙宁娜的哭诉、纳西妲的轮回、散兵的背叛、散兵的背叛、散兵的背叛、梅奔的春日影、董卓和吕布的爱情、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门、Rust入门……

4. 丐帮长小（赞 23）
感觉Rust很快会大爆发，以前主要是门槛高，写的费劲，现在有AI的强大支持，Rust的优势会得到大力发展。

5. 非花追月（赞 24）
[笑哭]再次入门。

6. ゙゚鸣潮（赞 18）
用Rust写了个everything-rs，但是还没写UI。另一个是剪切板，UI用的Slint。哪怕没学明白也能做点项目

7. 铷闪电一般（赞 7）
每次再次入门，我都只看生命周期[笑哭]这玩意我一听就会一写就废，特别是结构体跟生命周期联手时

8. 小灰宋外卖（赞 7）
建议来一期ai编程工具教程，让我们这些不懂的人，入门一下。

9. Aldridge12（赞 6）
又入门啊，果然是最适合入门的语言[doge]

10. bili_9145859（赞 5）
rust严格的语法约束，反而使得rust称为使用ai生成起来最稳定的预言

11. tiantiangugugaga（赞 4）
《AI写Rust入门》

铁锈的甜味在编译时弥漫，
所有权像新雪覆盖脚印。
我们借用指针的微光，
在栈与堆的间隙里游荡。

智能的盲人抚摸代码山脉，
每个分号都是悬崖勒马。
当模式匹配撕开逻辑的茧，
恐慌在控制台写下遗嘱。

哦，这语法森严的修道院，
连空白符都背诵清规。
而神经网络用概率的暖风，
轻叩那永远安全的——封闭的门。

铁锈的名字开出无瑕的花，
在电流的笔迹里涅槃。
[doge][doge][doge]

12. hihukayo（赞 4）
三过家门不入[笑哭]

13. 原来是Li哇（赞 4）
又来入门了

14. 亚尔特兔（赞 4）
图片是一个钉钉打卡统计工具(的核心函数)
我一不小心写太长了，耦合度似乎过高了，但我不知道该怎么拆[笑哭]
不过，考虑到过早优化是万恶之源，我打算保持现状了[喜极而泣]

15. Raisenburg（赞 3）
我还是回去看25年的视频吧，现在notebookllm快把B站占领了，听到notebookllm的声音就过敏，浑身不自在。而且notebookllm视频全是PPT风格，一张PPT配一段语音，实在是疲劳了，看到就难受，现在甚至看一眼封面就知道是notebookllm视频。

16. 冒泡喵泡（赞 3）
中文最好的rust老师[打call]

17. 坐尾骨（赞 3）
第三次入门

18. zeshur（赞 3）
太好了，又可以入门了[doge]

19. 没有哆啦A梦的犬雄（赞 4）
使用这个生成这个视频花了多少[蹲蹲]

20. 哥布林纯水（赞 2）
我问deepseek哪种技术你最熟悉，最容易使用
AI说是Typescript，
我问他它对rust掌握怎么样，它说因为比较新，
资料比较少，反而没有Typescript能帮我托底[笑哭]