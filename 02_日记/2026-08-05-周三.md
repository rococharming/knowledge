# 今日任务

- [x] Previewer 编排方案确定
- [ ] 热更新方案确定
- [ ] bdb npm 方案实施

# 跟踪日志

一、bdb npm 方案实施

1、可解压到任意目录直接运行的预览运行时包

-  打包脚本 build-runtime-bundle.sh：固定 Rust 1.97.1 + macOS 11.0，白名单收 uipreviewer、两个动态库、前端 gui/js 四类文件；动态库路径改成包内相对、去掉构建机绝对路径；ad-hoc 签名并严格校验；生成 manifest 记录源码版本、工具链指纹、文件哈希。
- 静态验证脚本 verify-bundle.py：9 项自动检查（架构、签名、依赖、路径、symlink 等），每项都做了反证确保能拦住坏数据。
- 启动验证脚本 verify-bundle-launch.py：解压到只读目录、清空环境变量，驱动真实启动到干净退出；分别验证 framework.snapshot 快速路径和删掉 snapshot 后的 framework.js fallback 兜底。



# 总结