# 今日任务

- [ ] Windows 应用不稳定修复
- [x] 手动点击调整详情页卡帧问题
- [x] npm 测试发包
- [ ] 推进热更新

# 跟踪日志

**一、当前分支进展：**

开发分支：`fix/windows-msvc-link`

两条主线：

1、 Windows

概述：将原先单一 `system_framework` 拆成分层 DLL 运行时，并重写独立 `run` 工具负责构建、部署、校验整套 DLL，解决 Windows PE 导出数量上限。macOS/Linux 仍采用单一 `system_framework`。
当前问题：
- jax 的 Windows `local` ABI 实现不完整，现在使用裸 rustc 临时绕过编译 app.dll
- 教主反馈的运行多次 hello 成功显示界面的次数很少，但本机没有复现

2、macOS Preview/BDB

概述：把旧 `ui-previewer` 中已验证的 Preview Session 能力迁入新仓，包括设备配置、`--app`、每个实例独立 session 数据目录、BDB WebSocket 自动化、折叠屏、无头模式。


# 总结